{
  "name": "multi-analysis-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n APP,API \"npm run dev --prefix app\" \"npm run dev --prefix api\"",
    "seed:mls": "node seed/mls-loader.js",
    "seed:rents": "node seed/rent-loader.js",
    "install:all": "npm install && npm install --prefix app && npm install --prefix api && npm install --prefix worker"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 